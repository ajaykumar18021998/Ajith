#include<stdio.h>
#include<conio.h>
int main()
{
int number,original number,remainder,result=0;
printf("enter an n integer:");
scanf("%e",&num);
original number=number;
while(original number!=0)
[
remainder=original number%10;
result=remainder*remainder*original number/=10;
}
if(result==number)
printf("%e is an armstrong number",number);
else
printf("%e is not an armstrong number",number);
return0;
}

    
