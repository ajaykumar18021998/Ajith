#iclude<stdio.h>
void main() 
{
int k,y,sum=0,num;
   printf(" Enter How many Number you want");
  scanf("%d",&x);
   printf(" Enter the elements one by one");
  for(i=0;i<y;++i)
   {
     scanf("%d",&num);
     Sum = Sum +num;
   return 0;
}
